defaults: &defaults
  adapter: postgresql
  username: postgres
  password: postgres
  host: localhost
  port: 5432
  pool: 10
  timeout: 5000

development:
  <<: *defaults
  database: hospitality_development

test:
  <<: *defaults
  database: hospitality_test


production:
  <<: *defaults
  database: hospitality_production
